// Number line route: single and double number line classroom display.
import { NumberLineBoard } from "@/components/NumberLineBoard";

export default function NumberLinePage() {
  return <NumberLineBoard />;
}
